//! Core configuration data types shared with the protocol crate.

pub use hotki_protocol::{FontWeight, NotifyPos, NotifyTheme, NotifyWindowStyle, Offset, Pos};

/// Display mode for the HUD.
pub type Mode = hotki_protocol::Mode;
