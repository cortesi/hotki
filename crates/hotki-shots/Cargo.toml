[package]
name = "hotki-shots"
version = "0.0.1"
edition = "2024"
publish = false

[dependencies]
clap = { workspace = true, features = ["derive"] }
hotki-app-session = { version = "0.1.0", path = "../smoketest" }
hotki-protocol = { path = "../hotki-protocol" }
